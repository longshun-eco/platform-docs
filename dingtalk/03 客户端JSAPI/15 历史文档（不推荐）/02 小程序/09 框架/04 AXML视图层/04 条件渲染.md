---
title: "条件渲染"
source: "https://open.dingtalk.com/document/development/conditional-rendering-1"
category: "客户端JSAPI / 历史文档（不推荐） / 小程序 / 框架 / AXML视图层"
updated: 
tags:
  - dingtalk
  - 客户端JSAPI
---
![[development-conditional-rendering-1_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-conditional-rendering-1_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2025-09-17钉钉小程序支持a:if和block a:if条件渲染。

## **a:if**

在框架中，我们使用 `a:if="{{condition}}"` 来判断是否需要渲染该代码块。

```
 True
```

也可以使用`a:elif`和`a:else`来添加一个`else`块。

```
 1
 2
 3
```

## **block a:if**

因为`a:if`是一个控制属性，需要将它添加到一个标签上。如果想一次性判断多个组件标签，可以使用一个 `<block/>` 标签将多个组件包装起来，并在它的上边使用`a:if`来控制属性。

```
   view1
   view2
```

**重要**

`<block/>` 并不是一个组件，仅仅是一个包装元素，不会在页面中做任何渲染，只接受控制属性。