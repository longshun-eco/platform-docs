---
title: "数据结构"
source: "https://open.dingtalk.com/document/development/data-structure"
category: "服务端API / AI表格"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-data-structure_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-data-structure_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2025-09-10该文档主要用于介绍AI表格中基础的数据结构。

AI表格中包含如下基本结构：

* Base，即一篇AI表格文档

  + `baseId`唯一标识了一篇AI表格文档。`baseId`可以通过以下方法获取。

    |  |  |  | | --- | --- | --- | | **通过URL获取**  ![[development-data-structure_p979041.png]] | **通过文档信息面板获取**  ![[development-data-structure_p979049.png]]  ![[development-data-structure_p979050.png]] | **通过其它API获取**  - 可通过[获取节点列表](/document/development/get-node-list)接口获取。 **说明**  创建AI表格时，`docType`值为`BASE`。 |
* Sheet（即数据表），一篇AI表格文档中通常有多个数据表，且至少有一个数据表。

  + 所有需要访问sheet的接口都可以使用`sheetId`或`sheetName`作为入参。
  + sheetId可以通过[获取所有数据表](/document/development/api-notable-getallsheets)接口获取。

    **说明**

    sheetId仅保证在文档中唯一，不保证全局唯一。
  + sheetName为前端可见的sheet名称，如下图中的`任务管理`。
* Field（即字段），数据表中的每一列即是一个字段，一个数据表中通常有多个字段。

  + 所有需要访问field的接口都可以使用`fieldId`或`fieldName`作为入参。
  + fieldId可以通过[获取所有字段](/document/development/api-noatable-getallfields)接口获取。

    **说明**

    fieldId仅保证在文档中唯一，不保证全局唯一。
  + fieldName为前端可见的field名称，如下图中的`重要程度`。
  + 每个sheet的第一列为「主字段」，其仅支持特定几种字段类型，且该列不可删除。具体支持的字段类型使用如下方法查看

    - 打开任意AI表格
    - 编辑主字段
    - 查看可设置的字段类型
* Record（即记录），数据表中的每一行即是一个记录，一个数据表中通常有多个记录。

  + 所有需要访问record接口需要使用`recordId`。
  + recordId可以通过[新增记录](/document/development/api-notable-insertrecords)或[列出多行记录](/document/development/api-notable-listrecords)等接口获取。

    **说明**

    recordId仅保证在文档中唯一，不保证全局唯一。

    ![[development-data-structure_p979051.png]]