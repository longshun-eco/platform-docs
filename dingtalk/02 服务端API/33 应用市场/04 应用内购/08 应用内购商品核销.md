---
title: "应用内购商品核销旧版SDK"
source: "https://open.dingtalk.com/document/development/application-of-in-house-purchase-verification"
category: "服务端API / 应用市场 / 应用内购"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-application-of-in-house-purchase-verification_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-application-of-in-house-purchase-verification_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2026-03-06通过本接口可在应用内购流程中对订购商品进行核销操作，适用于第三方SaaS服务商根据客户实际使用情况（如开通人数、使用时长等）定期核销已使用的订购额度的业务场景。每次成功调用将记录一条核销流水，确保订单使用状态可追溯。

### **使用场景**

该接口主要适用于以下典型业务场景：

* 第三方应用根据企业用户的实际使用人数（如每月激活用户数），按需核销对应的订购数量。
* 基于服务使用周期（如按月计费）定时批量核销已消耗的服务单元。

建议结合事件订阅机制监听相关变更事件，提升系统自动化能力。

## 权限

服务端API是以应用维度授权的，在调用接口前，确保已经为应用添加了接口权限。

|  |  |  |  |
| --- | --- | --- | --- |
| 应用类型 | 是否支持调用 | 权限申请方式 | API Explorer调试 |
| 企业内部应用 | 暂不支持 | 暂不支持 | 暂不支持 |
| 第三方企业应用 | 支持 | 开通应用在应用市场的内购订单的数据管理权限 | [调试](https://open-dev.dingtalk.com/apiExplorer#/?devType=isv&api=dingtalk.oapi.appstore.internal.order.consume) |
| 第三方个人应用 | 暂不支持 | 暂不支持 | 暂不支持 |

## 基本信息

**请求方式**：POST

**请求地址**：`https://oapi.dingtalk.com/topapi/appstore/internal/order/consume`

## Query参数

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 示例值 | 描述 |
| access\_token | String | 是 | 6d1bxxxx | 调用本接口的访问凭证，通过调用[服务商获取第三方应用授权企业的access\_token](/document/development/obtain-isvapp-token)接口获取。 |

## Body参数

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 示例值 | 描述 |
| biz\_order\_id | Number | 是 | 3131111111 | 内购商品订单号。 |
| request\_id | String | 是 | 1199291922 | 核销请求ID，由ISV生成，用于请求幂等。 |
| quantity | Number | 是 | 12 | 订购商品核销数量。 |
| userid | String | 是 | user123 | 员工在当前企业内的唯一标识，也称staffId。 |

## 返回参数

|  |  |  |  |
| --- | --- | --- | --- |
| 名称 | 类型 | 示例值 | 描述 |
| errmsg | String | 成功 | 返回码描述。 |
| errcode | Number | 0 | 返回码。 |

## 示例

**请求示例（HTTP）**

```http
POST https://oapi.dingtalk.com/topapi/appstore/internal/order/consume?access_token=ACCESS_TOKEN
```

请求正文

```json
{
        "quantity":"12",
        "biz_order_id":"3131111111",
        "request_id":"1199291922",
        "userid":"user123"
}
```

**请求示例（JAVA SDK）**

```java
DefaultDingTalkClient client = new DefaultDingTalkClient("https://oapi.dingtalk.com/topapi/appstore/internal/order/consume");
OapiAppstoreInternalOrderConsumeRequest request = new OapiAppstoreInternalOrderConsumeRequest();
request.setBizOrderId(3131111111L);
request.setQuantity(12L);
request.setRequestId("1199291922");
request.setUserid("user123");
OapiAppstoreInternalOrderConsumeResponse response = client.execute(request, access_token);
```

**返回示例**

```json
{
        "errcode":0,
        "errmsg":"成功"
}
```