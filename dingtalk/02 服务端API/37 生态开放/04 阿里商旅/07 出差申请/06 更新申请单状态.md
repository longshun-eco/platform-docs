---
title: "更新申请单状态旧版SDK"
source: "https://open.dingtalk.com/document/development/update-approval-form"
category: "服务端API / 生态开放 / 阿里商旅 / 出差申请"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-update-approval-form_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-update-approval-form_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2026-01-29通过此接口更新审批单的处理状态，适用于企业差旅、报销等场景中对申请单进行审批操作（如同意、拒绝、转交或取消）。

## 使用场景

本接口主要用于与钉钉集成的企业差旅管理系统中，当外部系统完成对某条申请单的审批逻辑后，需同步更新钉钉侧对应申请单的状态。典型业务场景包括：

* 审批流程自动化：第三方审批系统在完成内部审批后，调用该接口将结果回写至钉钉。
* 状态一致性维护：确保外部系统与钉钉平台之间的申请单状态保持一致。
* 操作留痕：记录审批人、操作时间及备注信息，便于后续审计和追溯。

**调用时机建议**：

* 在审批决策完成后立即调用，保证状态实时同步。

![[development-update-approval-form_p375500.png]]

## 权限

服务端API是以应用维度授权的，在调用接口前，确保已经为应用添加了接口权限。

|  |  |  |  |
| --- | --- | --- | --- |
| 应用类型 | 是否支持调用 | 权限申请方式 | API Explorer调试 |
| 企业内部应用 | 是 | 阿里商旅专用写入权限点 | [调试](https://open-dev.dingtalk.com/apiExplorer#/?devType=org&api=dingtalk.oapi.alitrip.btrip.approval.update) |
| 第三方企业应用 | 是 | 阿里商旅专用写入权限点 | [调试](https://open-dev.dingtalk.com/apiExplorer#/?devType=isv&api=dingtalk.oapi.alitrip.btrip.approval.update) |
| 第三方个人应用 | 否 | — | — |

## 基本信息

**请求方式**：POST

**请求地址**：`https://oapi.dingtalk.com/topapi/alitrip/btrip/approval/update`

## Query参数

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 示例值 | 描述 |
| access\_token | String | 是 | dc73axxxx | 调用该API的应用凭证。   * 企业内部应用，通过[获取企业内部应用的access\_token](/document/development/obtain-orgapp-token)接口获取。 * 第三方企业应用，通过[获取第三方企业的access\_token](/document/development/obtain-isvapp-token)接口获取。 |

## Body参数

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 示例值 | 描述 |
| rq | OpenApiUpdateApplyRq | 是 |  | 请求对象，封装所有更新参数。 |
| thirdpart\_apply\_id | String | 是 | 12345 | 外部申请单ID，调用[获取申请单列表](/document/development/search-enterprise-approval-form-data)接口获取。 |
| operate\_time | Date | 是 | 2017-01-01 00:00:00 | 操作时间。 |
| status | Number | 是 | 1 | 申请单状态：   * 1：已同意 * 2：已拒绝 * 3：已转交 * 4：已取消 |
| userid | String | 是 | user1 | 审批人的userid，需为钉钉系统内有效的用户标识。 |
| user\_name | String | 否 | 张三 | 审批人姓名。 |
| note | String | 否 | 同意 | 审批备注信息，可为空。 |
| corpid | String | 是 | corp1 | 企业的corpid，标识目标企业租户。 |

## 返回参数

|  |  |  |  |
| --- | --- | --- | --- |
| 名称 | 类型 | 示例值 | 描述 |
| errmsg | String | 成功 | 返回码描述。 |
| errcode | Number | 0 | 返回码。 |
| success | Boolean | true | 操作是否成功。 |

## 示例

**请求示例（HTTP）**

```http
POST https://oapi.dingtalk.com/topapi/alitrip/btrip/approval/update?access_token=ACCESS_TOKEN
```

请求正文

```json
{
        "rq":{
                "note":"同意",
                "corpid":"corp1",
                "thirdpart_apply_id":"12345",
                "user_name":"张三",
                "userid":"user1",
                "operate_time":"2017-01-01 00:00:00",
                "status":"1"
        }
}
```

**请求示例（JAVA SDK）**

```java
DingTalkClient client = new DefaultDingTalkClient("https://oapi.dingtalk.com/topapi/alitrip/btrip/approval/update");
OapiAlitripBtripApprovalUpdateRequest req = new OapiAlitripBtripApprovalUpdateRequest();
OpenApiUpdateApplyRq obj1 = new OpenApiUpdateApplyRq();
obj1.setThirdpartApplyId("12345");
obj1.setOperateTime(StringUtils.parseDateTime("2017-01-01 00:00:00"));
obj1.setStatus(1L);
obj1.setUserid("user1");
obj1.setUserName("张三");
obj1.setNote("同意");
obj1.setCorpid("corp1");
req.setRq(obj1);
OapiAlitripBtripApprovalUpdateResponse rsp = client.execute(req, access_token);
System.out.println(rsp.getBody());
```

**返回示例**

```json
{
        "errcode":"0",
        "success":"true",
        "errmsg":"成功"
}
```