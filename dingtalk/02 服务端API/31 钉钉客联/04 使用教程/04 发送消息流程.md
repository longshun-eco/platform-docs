---
title: "发送消息流程"
source: "https://open.dingtalk.com/document/development/message-sending-process"
category: "服务端API / 钉钉客联 / 使用教程"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-message-sending-process_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-message-sending-process_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2025-09-24本文档介绍了如何调用钉钉客联消息接口发送消息。

## **流程简介**

前提条件：完成[创建应用](/document/dingstart/create-application)的流程且已经开通了钉钉客联。

步骤一：获取应用凭证信息，获取应用 Client ID 和 Client Secret。

步骤二：申请接口权限，申请钉钉客联相关接口的权限。

步骤三：获取应用访问凭证[获取企业内部应用的accessToken](/document/development/obtain-the-access-token-of-an-internal-app)。调用接口时，通过accessToken鉴权调用者身份。

步骤四：调用钉钉客联相关API：

1. 钉钉客联发送消息：

   * 调用服务端API-[在钉钉客联互通群中使用钉外账号发送消息](/document/development/send-c2b-messages)接口。
   * 获取用户登录凭证，调用服务端API-[在钉钉客联互通群中使用钉内账号发送消息](/document/development/send-b2c-messages)接口。
   * 调用服务端API-[在钉钉客联互通群中使用机器人发送消息](/document/development/group-robots-send-messages)接口。
2. 调用服务端API-[查询钉钉客联钉外账号未读消息数](/document/development/querying-the-number-of-unread-messages-of-the-user)接口。

## **前提条件**

完成[创建应用](/document/dingstart/create-application)的流程。

## **步骤一：获取应用凭证**

1. 选择目标应用，进入应用详情页，单击**基础信息** > **凭证与基础信息**。
2. 获取应用 Client ID 和 Client Secret。

## 步骤二：添加接口权限

单击**开发配置** > **权限管理**，在权限搜索框中输入`InterConnect.Common.ReadWrite`，并申请权限。

## **步骤三：获取应用访问凭证accessToken**

**重要**

服务端API差异详情参见[旧版服务端API、新版服务端API的区别](/document/development/how-to-call-apis#section-8lr-id4-rbz)。以下接口均使用服务端API接口，SDK下载详情参见[服务端SDK下载](/document/development/download-the-server-side-sdk)。

根据步骤一中的 Client ID 和 Client Secret，获取应用访问凭证[获取企业内部应用的accessToken](/document/development/obtain-the-access-token-of-an-internal-app)。

```java
public void getAccessToken() throws Exception {
        Config config = new Config();
        config.protocol = "https";
        config.regionId = "central";
        com.aliyun.dingtalkoauth2_1_0.Client client = new com.aliyun.dingtalkoauth2_1_0.Client(config);
        GetAccessTokenRequest accessTokenRequest = new GetAccessTokenRequest()
                .setAppKey("din*********hgn")
                .setAppSecret("9G_O************mBkhgGIO");
        GetAccessTokenResponse accessToken = client.getAccessToken(accessTokenRequest);
        System.out.println(JSON.toJSONString(accessToken.getBody()));
    }
```

## **步骤四：调用服务端相关API**

1. 钉钉钉客联发送消息：

   * 调用服务端API-[在钉钉客联互通群中使用钉外账号发送消息](/document/development/send-c2b-messages)接口。

     ```java
     public void sendMessage() throws Exception {
       Config config = new Config();
       config.protocol = "https";
       config.regionId = "central";
       com.aliyun.dingtalkim_1_0.Client client = new com.aliyun.dingtalkim_1_0.Client(config);
       SendMessageHeaders sendMessageHeaders = new SendMessageHeaders();
       sendMessageHeaders.xAcsDingtalkAccessToken = "";
       SendMessageRequest sendMessageRequest = new SendMessageRequest()
         .setSenderId("1107****2120")
         .setReceiverId("1745****8777")
         .setOpenConversationId("14da****2760")
         .setMessageType("text")
         .setMessage("{ \"text\": { \"content\":\"hello world\" } }");

       try {
         SendMessageResponse response = client.sendMessageWithOptions(sendMessageRequest, sendMessageHeaders, new RuntimeOptions());
         System.out.println(JSON.toJSONString(response.getBody()));
       } catch (TeaException err) {
         if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
           System.out.println(err.code);
           System.out.println(err.message);
     ```
   * 获取用户登录凭证，调用服务端API-**钉内用户身份向互通群或钉外用户发送消息**接口。

     1. [获取登录用户的访问凭证](/document/development/obtain-identity-credentials)，访问登录授权页面，获取`authCode`。
     2. 调用服务端API-[在钉钉客联互通群中使用钉内账号发送消息](/document/development/send-b2c-messages)接口。

        ```java
        public void sendDingMessage() throws Exception {
          Config config = new Config();
          config.protocol = "https";
          config.regionId = "central";
          com.aliyun.dingtalkim_1_0.Client client = new com.aliyun.dingtalkim_1_0.Client(config);
          SendDingMessageHeaders sendDingMessageHeaders = new SendDingMessageHeaders();
          sendDingMessageHeaders.xAcsDingtalkAccessToken = "";
          SendDingMessageRequest sendDingMessageRequest = new SendDingMessageRequest()
            .setSenderId("1745****8777")
            .setReceiverId("1107****2120")
            .setOpenConversationId("14da****2760")
            .setMessageType("text")
            .setMessage("{\"text\": {\"content\": \"hello world\"} }")
            .setCode("06f4****d1ec");
          try {
            SendDingMessageResponse response = client.sendDingMessageWithOptions(sendDingMessageRequest, sendDingMessageHeaders, new RuntimeOptions());
            System.out.println(JSON.toJSONString(response.getBody()));
          } catch (TeaException err) {
            if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
              System.out.println(err.code);
              System.out.println(err.message);
        ```
   * 调用服务端API-[在钉钉客联互通群中使用机器人发送消息](/document/development/group-robots-send-messages)接口。

     ```java
     public void sendRobotMessage() throws Exception {
       Config config = new Config();
       config.protocol = "https";
       config.regionId = "central";
       com.aliyun.dingtalkim_1_0.Client client = new com.aliyun.dingtalkim_1_0.Client(config);
       SendRobotMessageHeaders sendRobotMessageHeaders = new SendRobotMessageHeaders();
       sendRobotMessageHeaders.xAcsDingtalkAccessToken = "";
       SendRobotMessageRequest sendRobotMessageRequest = new SendRobotMessageRequest()
         .setOpenConversationIds(java.util.Arrays.asList(
           "1745****8777"
         ))
         .setMsgType("text")
         .setMsgContent("{ \"content\": \"我就是我, 是不一样的烟火\"}")
         .setAtDingUserId("1107****2120")
         .setAtAppUserId("1107****2120")
         .setAtAll(true);

       try {
         SendRobotMessageResponse response = client.sendRobotMessageWithOptions(sendRobotMessageRequest, sendRobotMessageHeaders, new RuntimeOptions());
         System.out.println(JSON.toJSONString(response.getBody()));
       } catch (TeaException err) {
     ```
2. 调用服务端API-[查询钉钉客联钉外账号未读消息数](/document/development/querying-the-number-of-unread-messages-of-the-user)接口。

   ```java
   public void queryUnReadMessage() throws Exception {
     Config config = new Config();
     config.protocol = "https";
     config.regionId = "central";
     com.aliyun.dingtalkim_1_0.Client client = new com.aliyun.dingtalkim_1_0.Client(config);
     QueryUnReadMessageHeaders queryUnReadMessageHeaders = new QueryUnReadMessageHeaders();
     queryUnReadMessageHeaders.xAcsDingtalkAccessToken = "";
     QueryUnReadMessageRequest queryUnReadMessageRequest = new QueryUnReadMessageRequest()
       .setAppUserId("1107****2120")
       .setOpenConversationIds(java.util.Arrays.asList(
         "14da****2760"
       ));
     try {
       QueryUnReadMessageResponse response = client.queryUnReadMessageWithOptions(queryUnReadMessageRequest, queryUnReadMessageHeaders, new RuntimeOptions());
       System.out.println(JSON.toJSONString(response.getBody()));
     } catch (TeaException err) {
       if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
         System.out.println(err.code);
         System.out.println(err.message);
       }
     } catch (Exception _err) {
   ```