---
title: "获取用户基本信息旧版SDK"
source: "https://open.dingtalk.com/document/development/queries-basic-user-information"
category: "服务端API / 通讯录管理"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-queries-basic-user-information_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-queries-basic-user-information_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2025-09-10调用本接口通过免登授权码获取用户基本信息。

## 基本信息

**请求方式**：POST（HTTPS）

**请求地址**：`https://oapi.dingtalk.com/sns/getuserinfo_bycode`

## Query参数

|  |  |  |  |
| --- | --- | --- | --- |
| 参数 | **参数类型** | 是否必选 | 描述 |
| accessKey | String | 是 | 应用的AppId。 |
| timestamp | String | 是 | 当前时间戳, 单位毫秒。 |
| signature | String | 是 | 用timestamp+"\n"+AppSecret为签名字符串，AppSecret为签名密钥，使用算法HmacSHA256计算的签名值。 |

## Body参数

|  |  |  |  |
| --- | --- | --- | --- |
| 参数 | 参数类型 | 是否必选 | 描述 |
| tmp\_auth\_code | String | String | 用户授权给钉钉开放应用的免登授权码，通过调用JSAPI `dd.getAuthCode`获取，参考[免登授权码](/document/development/mini-program-free-login)。 |

## 示例

**请求示例（HTTPS）**

```http
POST https://oapi.dingtalk.com/sns/getuserinfo_bycode?accessKey=ACCESSKEY&timestamp=TIMSSTAMP&signature=SIGNATURE
```

**请求正文**

```json
{
    "tmp_auth_code": "23152698ea18304da4d0ce1xxxxx"
}
```

**请求示例（JAVA SDK）**

```java
DefaultDingTalkClient  client = new DefaultDingTalkClient("https://oapi.dingtalk.com/sns/getuserinfo_bycode");
// 如果您使用的SDK中不存在该类，请下载最新的SDK
OapiSnsGetuserinfoBycodeRequest req = new OapiSnsGetuserinfoBycodeRequest();
req.setTmpAuthCode("4a2c5695b78738d495f47b5fee9160cd");
OapiSnsGetuserinfoBycodeResponse response = client.execute(req,"yourAppId","yourAppSecret");
```

**返回示例**

```json
{
     "errcode": 0,
     "errmsg": "ok",
     "user_info": {
          "nick": "杨XX",
          "unionid": "Wzh9KbyaCZxxx",
          "openid": "EtiiaAu3Yf9Ruxxx",
          "main_org_auth_high_level": true
     }
}
```

## **错误码**

|  |  |
| --- | --- |
| **错误码** | **说明** |
| 853002 | timestamp参数不合法，该参数值与钉钉服务器当前时间相差超过1分钟 |
| 853001 | timestamp参数格式不正确，必须是当前时间的毫秒数 |
| 853003 | accessKey参数不合法，必须是钉钉开放平台存在的appId |
| 853004 | signature参数不正确，与钉钉服务端计算出来的签名不一致，注意该参数传递时必须urlEncode |