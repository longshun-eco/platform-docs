---
title: "同意或拒绝审批流"
source: "https://open.dingtalk.com/document/development/agree-to-or-deny-the-approval-flow"
category: "服务端API / 历史文档（不推荐） / OA审批 / API案例"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-agree-to-or-deny-the-approval-flow_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-agree-to-or-deny-the-approval-flow_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2025-09-08本教程介绍了官方OA审批如何发起及操作审批流程。

## 预期效果

### 同意审批节点

![[development-agree-to-or-deny-the-approval-flow_p478193.png]]

### 拒绝审批节点

![[development-agree-to-or-deny-the-approval-flow_p478197.png]]

## 接入流程简介

本文档展示了，创建一个企业内部应用，使用**官方OA审批**实现发起审批、操作审批等流程：

步骤一：登录[开发者后台](https://open-dev.dingtalk.com/#/)，点击应用开发-企业内部开发，创建[企业内部应用](/document/app/create-orgapp#topic-2024342)。

步骤二：单击基础信息 > 应用信息，获取应用AppKey和AppSecret。

步骤三：[添加接口调用权限](/document/orgapp/add-api-permission)，单击“OA审批”，申请OA审批对应的权限。

步骤四：获取应用访问凭证[获取企业内部应用的access\_token](/document/orgapp/obtain-orgapp-token)。调用接口时，通过accessToken鉴权调用者身份。

步骤五：调用OA审批相关API：

1. 调用服务端API-[创建或更新审批模板](/document/orgapp/create-or-update-approval-templates)接口，获取模板的唯一编码`process_code`。
2. 根据模板编码`process_code`，调用服务端API-[发起审批实例](/document/orgapp/initiate-approval)接口发起审批实例，获取审批实例`process_instance_id`。
3. 根据审批实例`process_instance_id`，调用服务端API-[获取单个审批实例详情](/document/orgapp/obtains-the-details-of-a-single-approval-instance)接口，获取审批实例详情，获取审批任务各个任务节点信息`task_id`。
4. 根据审批实例`process_instance_id`和相应的任务节点`task_id`信息，调用服务端API-[同意或拒绝审批任务](/document/orgapp/execute-approval-operation-with-attachment)，实现审批任务的操作，所有审批节点同意后，则该审批单通过。该步骤不支持第三方企业应用。

## 步骤一：创建企业内部应用

**说明**

如果已有企业内部应用，可直接使用已有应用，可忽略此步骤。

1. 登录[开发者后台](https://open-dev.dingtalk.com/#/)，创建[企业内部应用](/document/orgapp/create-orgapp)。

   * 应用类型：选择H5微应用。
   * 开发方式：选择企业自主开发。

     ![[development-agree-to-or-deny-the-approval-flow_p477411.png]]

## 步骤二：获取AppKey和AppSecret

获取应用AppKey和AppSecret信息。

![[development-agree-to-or-deny-the-approval-flow_p477413.png]]

## 步骤三：添加接口权限

[添加接口调用权限](/document/orgapp/add-api-permission)。单击“OA审批”，申请OA审批对应的权限。

![[development-agree-to-or-deny-the-approval-flow_p477417.png]]

## 步骤四：获取应用访问凭证accessToken

**重要**

服务端API差异详情参见[新旧版规范服务端API区别](/document/orgapp/differences-between-server-apis-and-new-server-apis)。

* 服务端API接口SDK下载，详情参见[服务端SDK下载](/document/orgapp/download-the-server-side-sdk)。
* 新版服务端API接口SDK下载，详情参见[新版服务端SDK下载](/document/orgapp/sdk-download)。

以下接口均使用服务端API接口，SDK下载详情参见[服务端SDK](/document/orgapp/download-the-server-side-sdk)。

根据步骤二中的AppKey和AppSecret，获取应用访问凭证[获取企业内部应用的access\_token](/document/orgapp/obtain-orgapp-token)。

```java
public void getAccessToken() throws ApiException {
        DingTalkClient client = new DefaultDingTalkClient("https://oapi.dingtalk.com/gettoken");
        OapiGettokenRequest req = new OapiGettokenRequest();
        req.setAppkey("dingxxxxxxxxxhgn");
        req.setAppsecret("9G_xxxxxxxxxxxxxxx1JDf0Qq3nexxxxxxxxGIO");
        req.setHttpMethod("GET");
        OapiGettokenResponse rsp = client.execute(req);
        System.out.println(rsp.getBody());
    }
```

## 步骤五：调用服务端OA相关API

1. 调用服务端API-[创建或更新审批模板](/document/orgapp/create-or-update-approval-templates)接口，获取模板的唯一编码`process_code`。

   **说明**

   若没有保存接口返回的模板编码`process_code`，钉钉管理后台版本不同，获取processCode的方式不同。登录钉钉管理后台，在首页查看版本。如下图所示，页面展示**回到旧版**和**新版反馈**，说明当前是新版。

   ![[development-agree-to-or-deny-the-approval-flow_p381720.png]]

   **新版钉钉管理后台**：在审批模板编辑页-基础设置-**页面底部**查看。

   ![[development-agree-to-or-deny-the-approval-flow_p443734.png]]

   **旧版钉钉管理后台**：在审批模板编辑页的URL中查看。

   ![[development-agree-to-or-deny-the-approval-flow_p344894.png]]

   ```java
     public void createTemplate() throws ApiException {
           DingTalkClient client = new DefaultDingTalkClient("https://oapi.dingtalk.com/topapi/process/save");
           OapiProcessSaveRequest request = new OapiProcessSaveRequest();
           OapiProcessSaveRequest.SaveProcessRequest saveProcessRequest = new OapiProcessSaveRequest.SaveProcessRequest();
           saveProcessRequest.setName("创建审批模板");
           //有参数processCode时为更新模板，无参数processCode时，为创建模板，该示例为创建模板
           //saveProcessRequest.setProcessCode("PROC-37801FB3-xxxx-xxxx-xxxx-6C6ADB87CD9A");
           saveProcessRequest.setAgentid(118xxxxxx5L);
           saveProcessRequest.setDescription("创建审批模板");

           // 注意，每种表单组件，对应的componentName是固定的，参照以下示例代码
           List formComponentList = new ArrayList<>();

           //单行文本框
           OapiProcessSaveRequest.FormComponentVo singleInput = new OapiProcessSaveRequest.FormComponentVo();
           singleInput.setComponentName("TextField");
           OapiProcessSaveRequest.FormComponentPropVo singleInputProp = new OapiProcessSaveRequest.FormComponentPropVo();
           singleInputProp.setRequired(true);
           singleInputProp.setLabel("单行输入框");
           singleInputProp.setPlaceholder("请输入");
           singleInputProp.setId("TextField-J78F056R");
   ```
2. 根据模板编码`process_code`，调用服务端API-[发起审批实例](/document/orgapp/initiate-approval)接口发起审批实例，获取审批实例`process_instance_id`。

   ```java
   public void createProcessInstance() throws ApiException {
           DingTalkClient client = new DefaultDingTalkClient("https://oapi.dingtalk.com/topapi/processinstance/create");
           OapiProcessinstanceCreateRequest req = new OapiProcessinstanceCreateRequest();
           //应用标识，可在开发者后台的应用详情页获取。
           req.setAgentId(115xxxxx675L);
           req.setProcessCode("PROC-6xxxxxx-Bxxx-4xxxx-Bxxxx-CD2xxxxxB28C");
           //审批实例发起人的userid。
           req.setOriginatorUserId("01472xxxxxxxx77041");
           //发起人所在的部门，如果发起人属于根部门，传-1。
           req.setDeptId(-1L);
           //抄送人userid列表，最大列表长度20。
           req.setCcList("manxxxxx75,08521xxxxxx84272");
           //在审批结束后发送抄送
           req.setCcPosition("FINISH");

           //单行输入框
           List formComponentValueVoList = new ArrayList();
           OapiProcessinstanceCreateRequest.FormComponentValueVo formComponentValueVo = new OapiProcessinstanceCreateRequest.FormComponentValueVo();
           formComponentValueVoList.add(formComponentValueVo);
           formComponentValueVo.setName("单行输入框");
           formComponentValueVo.setValue("测试单行输入框");
   ```
3. 根据审批实例`process_instance_id`，调用服务端API-[获取单个审批实例详情](/document/orgapp/obtains-the-details-of-a-single-approval-instance)接口，获取审批实例详情，获取审批任务各个任务节点信息`task_id`。

   ```java
    public void processInstanceInfo() throws ApiException {
           DingTalkClient client = new DefaultDingTalkClient("https://oapi.dingtalk.com/topapi/processinstance/get");
           OapiProcessinstanceGetRequest req = new OapiProcessinstanceGetRequest();
           //审批实例process_instance_id
           req.setProcessInstanceId("T-0rxjjHS-ixxxxxxI4_A08811660537111");
           OapiProcessinstanceGetResponse rsp = client.execute(req, "access_token");
           System.out.println(rsp.getBody());
       }
   ```
4. 根据审批实例`process_instance_id`和相应的任务节点`task_id`信息，调用服务端API-[同意或拒绝审批任务](/document/orgapp/execute-approval-operation-with-attachment)，实现审批任务的操作，所有审批节点同意后，则该审批单通过。该步骤不支持第三方企业应用。

   **重要**

   需按照审批节点顺序的`task_id`执行。

   以本流程为例：发起审批时设置先设置了会签，然后设置了或签两个审批节点，必须先执行会签中的`task_id`任务节点，否则接口将无法调用成功。

   ```java
    public void executeProcessInstance() throws ApiException {
           DingTalkClient client = new DefaultDingTalkClient("https://oapi.dingtalk.com/topapi/process/instance/execute");
           OapiProcessinstanceExecuteV2Request req = new OapiProcessinstanceExecuteV2Request();
           OapiProcessinstanceExecuteV2Request.ExecuteTaskRequest executeTaskRequest = new OapiProcessinstanceExecuteV2Request.ExecuteTaskRequest();
           executeTaskRequest.setProcessInstanceId("T-0rxjjHS-ixxxxxxI4_A08811660537111");
           executeTaskRequest.setActionerUserid("085***********272");
           executeTaskRequest.setTaskId(756******18L);
           executeTaskRequest.setRemark("同意");
           executeTaskRequest.setResult("agree");
           req.setRequest(executeTaskRequest);
           OapiProcessinstanceExecuteV2Response rsp = client.execute(req, "access_token");
           System.out.println(rsp.getBody());
       }
   ```