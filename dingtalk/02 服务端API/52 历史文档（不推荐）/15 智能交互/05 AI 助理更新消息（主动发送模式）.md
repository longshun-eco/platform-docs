---
title: "AI 助理更新消息（主动发送模式）新版SDK"
source: "https://open.dingtalk.com/document/development/the-ai-assistant-updates-active-message-sending-mode"
category: "服务端API / 历史文档（不推荐） / 智能交互"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-the-ai-assistant-updates-active-message-sending-mode_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-the-ai-assistant-updates-active-message-sending-mode_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2026-03-06开发者可以通过钉钉 AI 助理主动给用户发送自定义智能消息。主动发送的步骤可以分为：[预备](/document/development/api-prepare)、更新和[结束](/document/development/api-finish)三步。本文档介绍的接口是更新接口，该接口可以用会话凭证，更新相应消息框，且支持多次更新。

**重要**

本文档已于 2026年 03 月 05 日迁移至历史文档（不推荐）目录，且本接口仅保持现有功能，不再新增支持其他能力。

## 权限

要调用此API，需要以下权限之一。

|  |  |  |  |
| --- | --- | --- | --- |
| 应用类型 | 是否支持 | 权限 | API Explorer调试 |
| 企业内部应用 | 支持 | **重要**  暂不支持新增申请 | — |
| 第三方企业应用 | 暂不支持 | 暂不支持 | 暂不支持 |
| 第三方个人应用 | 暂不支持 | 暂不支持 | 暂不支持 |

## 请求方法

```sql
POST /v1.0/aiInteraction/update HTTP/1.1
Host:api.dingtalk.com
x-acs-dingtalk-access-token:String
Content-Type:application/json

{
  "conversationToken" : "String",
  "contentType" : "String",
  "content" : "String"
}
```

## Header参数

|  |  |  |  |
| --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 描述 |
| x-acs-dingtalk-access-token | String | 是 | 调用该接口的访问凭证，通过调用[获取企业内部应用的accessToken](/document/development/obtain-the-access-token-of-an-internal-app)接口获取。 |

## Body参数

|  |  |  |  |
| --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 描述 |
| conversationToken | String | 是 | 会话凭证。  **说明**  会话凭证中保存了会话的上下文信息，包括：聊天场景（如单聊、群聊等）、聊天对象（如 AI 助理、机器人等）、用户信息等。 |
| contentType | String | 是 | 消息体类型：   * **ai\_card**：AI 卡片 |
| content | String | 是 | 消息体内容，与消息体类型对应，必须是 **JSON** 格式，详见[消息体内容填写指南](/document/development/message-content-filling-guidance)。 |

## 参数补充说明

1. 如果你的`contentType`是`ai_card`，你可以这么填写`content` ：

```json
{
  "templateId" : String,  // 必填，卡片模板ID
  "cardData" : Map, // 卡片内容
}
```

示例

```json
{
  "templateId" : "xxxx",  // 必填，卡片模板ID
  "cardData" : "{\"title\":\"我是标题\",\"desc\":\"我是描述。\"}", // 卡片内容
}
```

## 返回参数

|  |  |  |
| --- | --- | --- |
| 名称 | 类型 | 描述 |
| result | Object | 请求结果。 |
| success | Boolean | 接口调用是否成功。 |

## 示例

**请求示例**

HTTP

```sql
POST /v1.0/aiInteraction/update HTTP/1.1
Host:api.dingtalk.com
x-acs-dingtalk-access-token:82c135ec59ee39579ca39787786c54aa
Content-Type:application/json

{
  "conversationToken" : "ctxxxx",
  "contentType" : "ai_card",
  "content" : "{\"templateId\": \"xxxx-xxxxx-xxxx-xxxx.schema\",\"cardData\": {\"title\":\"我是标题\",\"desc\":\"我是描述。\"}}"
}
```

Java

```
// This file is auto-generated, don't edit it. Thanks.
```

Python

```
# -*- coding: utf-8 -*-
```

PHP

```
php</code
```

Go

```
// This file is auto-generated, don't edit it. Thanks.
```

Node.js

```
'use strict';
```

C#

```
// This file is auto-generated, don't edit it. Thanks.
```

**返回示例**

```
HTTP/1.1 200 OK
Content-Type:application/json

{
  "result" : {
    "success" : true
  }
}
```

## 错误码

|  |  |  |  |
| --- | --- | --- | --- |
| HttpCode | 错误码 | 错误信息 | 说明 |
| 400 | invalid.conversation.token | invalid conversation token | 不合法的会话凭证。 |
| 400 | invalid.content.type | invalid content type | 消息体类型不合法。 |
| 400 | invalid.content | card template not exist | 消息体内容不合法，卡片模板不存在。 |
| 400 | invalid.content | emplateId cannot be null or empty | 消息体内容不合法，卡片模板不能为空。 |
| 400 | invalid.content | templateId in content is not equal to templateId in context | 消息体内容不合法，卡片模板需要全流程一致。 |
| 400 | invalid.content | card streaming key invalid | 消息体内容不合法，流式组件的 Key 不合法。 |
| 400 | invalid.content | invalid componentTag | 消息体内容不合法，卡片组件类型不合法。 |
| 500 | system.error | system error | 未知的系统内部错误。 |