---
title: "获取AI助理的消息列表新版SDK"
source: "https://open.dingtalk.com/document/development/api-listassistantmessage"
category: "服务端API / 历史文档（不推荐） / AI 助理 / 消息"
updated: 
tags:
  - dingtalk
  - 服务端API
---
![[development-api-listassistantmessage_O1CN01Zx4PED22rkUkmRe1L_!!6000000007174-2-tps-226-40.png]]![[development-api-listassistantmessage_O1CN01UwOv1C27lrTuGFj2D_!!6000000007838-2-tps-17-30.png]]

更新于 2026-03-06根据线程id，找到关联线程的消息，如果提供运行任务id，则只查找运行任务关联的消息，并根据限制数量和排序方式，返回对应的消息列表。

**重要**

本文档已于 2026年 03 月 05 日迁移至历史文档（不推荐）目录，且本接口仅保持现有功能，不再新增支持其他能力。

## 权限

要调用此API，需要以下权限之一。

|  |  |  |  |
| --- | --- | --- | --- |
| 应用类型 | 是否支持 | 权限 | API Explorer调试 |
| 企业内部应用 | 支持 | **重要**  暂不支持新增申请 | — |
| 第三方企业应用 | 暂不支持 | 暂不支持 | 暂不支持 |
| 第三方个人应用 | 暂不支持 | 暂不支持 | 暂不支持 |

## 请求方法

```
GET /v1.0/assistant/threads/{threadId}/messages?limit=Integerℴ=String&runId=String HTTP/1.1
Host:api.dingtalk.com
x-acs-dingtalk-access-token:String
Content-Type:application/json
```

## Header参数

|  |  |  |  |
| --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 描述 |
| x-acs-dingtalk-access-token | String | 是 | 调用该接口的访问凭证，通过调用[获取企业内部应用的accessToken](/document/development/obtain-the-access-token-of-an-internal-app)接口获取。 |

## Path参数

|  |  |  |  |
| --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 描述 |
| threadId | String | 是 | 消息关联的线程 ID。 |

## Query参数

|  |  |  |  |
| --- | --- | --- | --- |
| 名称 | 类型 | 是否必填 | 描述 |
| limit | Integer | 否 | 获取消息的数量，范围在1-100之间，不传默认为20。 |
| order | String | 否 | 获取消息的排序方式，枚举值：   * **asc**：升序排序 * **desc**：降序排序   **说明**  不传默认降序排序。 |
| runId | String | 否 | 消息关联的运行id，不传默认获取当前线程id下的所有消息。 |

## 返回参数

|  |  |  |
| --- | --- | --- |
| 名称 | 类型 | 描述 |
| data | Array | 返回的消息列表。 |
| id | String | 消息 ID。 |
| threadId | String | 消息关联的线程 ID。 |
| createdAt | Long | 消息创建时间，毫秒时间戳。 |
| role | String | 消息角色，枚举值：   * user：用户 * assistant：助理 * tool：技能 |
| content | Array of Any | 返回的消息内容，如果消息格式是text，则其中map的key值就是text，其中的value对应的是user或者assistant、tool角色的文本内容，annotations为消息的扩展字段，其中的tool表示运行过程中会调用的工具名称。 |
| assistantId | String | 消息关联的助理 ID。 |
| runId | String | 消息关联的运行任务 ID。 |
| metadata | Map | 保留字段，用于扩展使用，暂无实际意义。 |
| object | String | 对象类型，默认为 thread.message。 |
| object | String | 对象类型，默认为 list。 |

## 示例

**请求示例**

HTTP

```
GET /v1.0/assistant/threads/thread_message_95f***abb/messages?limit=20ℴ=desc&runId=run_73d***f2c HTTP/1.1
Host:api.dingtalk.com
x-acs-dingtalk-access-token:token123
Content-Type:application/json
```

Java

```
// This file is auto-generated, don't edit it. Thanks.
```

Python

```
# -*- coding: utf-8 -*-
```

PHP

```
php</code
```

Go

```
// This file is auto-generated, don't edit it. Thanks.
```

Node.js

```
'use strict';
```

C#

```
// This file is auto-generated, don't edit it. Thanks.
```

**返回示例**

```
HTTP/1.1 200 OK
Content-Type:application/json

{
  "data" : [ {
    "id" : "message_801***c52a",
    "threadId" : "thread_message_95f***abb",
    "createdAt" : 1721980599206,
    "role" : "assistant",
    "content" : [ {
      "type" : "text",
      "text" : {
        "value" : "我将为你写一本小说",
        "annotations" : {
          "tool" : "writer"
        }
      }
    } ],
    "assistantId" : "d748***965",
    "runId" : "run_73d***f2c",
    "object" : "thread.message"
```

## 错误码

|  |  |  |  |
| --- | --- | --- | --- |
| HttpCode | 错误码 | 错误信息 | 说明 |
| 500 | message.invalid.param | %s | 传入的参数非法(E000036) |
| 500 | message.system.error | %s | 系统异常(E000023) |
| 500 | message.limit.invalid | %s | 查询Message实例数目的配置值不合法， 单次查询不允许超过100个（E000156） |
| 500 | message.order.invalid | %s | 传入的order参数不合法，只允许“asc”和”desc“，请检查后重试(E000157) |
| 500 | message.threadId.invalid | %s | 获取消息列表时，传入的ThreadId不合法或者该Thread实例不存在（E000159） |
| 500 | message.list.exception | %s | 系统异常，请稍后重试(E000208) |
| 500 | message.list.error | %s | 系统异常，请稍后重试（E000032） |
| 500 | message.list.throwable | %s | 系统异常，请稍后重试（E000131） |